# webpack_test

``` bash
# if webpack command doesn't work, install npm pkg globally
npm install webpack -g
npm install -D webpack-cli

# Webpack command
webpack script-1.js -o bundle.js (to run before defining webpack config file)
webpack ( to run after adding webpack config)

# Atom IDE installed packages
atom-live-server
shortcut >> ctrl + alt + 3 (localhost:3000)

platform-ide-terminal
